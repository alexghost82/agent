rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through authenticated Cloud Functions (Admin SDK), which
    // enforce per-user data isolation. Direct client access is denied as a
    // defense-in-depth measure.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
